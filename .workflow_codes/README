

# Description

Contains cpp driver and Python specialization for the workflow in: `https://github.com/Pressio/surrogates-comparison`.

## Building the C++ code:

```bash
export CXX=<cpp14-compiler>
# assuming we are inside pressio-demoapps/workflow_codes
mkdir build && cd build
cmake ..
```